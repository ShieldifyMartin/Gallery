import { createStore } from 'vuex';
import authentication from './authModule';

export default createStore({
  state: {
    modules: {
      authentication
    }
  },
  getters: {    
  },
  mutations: {    
  },
  actions: {
  },
  modules: {
  }
})
